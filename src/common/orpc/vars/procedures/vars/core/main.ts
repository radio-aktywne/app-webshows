import { events } from "./vars/events";
import { instances } from "./vars/instances";
import { shows } from "./vars/shows";

export const core = {
  events: events,
  instances: instances,
  shows: shows,
};
